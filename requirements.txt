# S3 (Deka Box) upload/download
boto3==1.43.51
python-dotenv==1.0.0

# OCR + PDF parsing
docling==2.113.0

# Embeddings (local model, downloads on first use)
langchain-huggingface==0.3.1
sentence-transformers>=2.7.0

# Vector store on PostgreSQL + pgvector
langchain-postgres==0.0.17
pgvector>=0.2.5,<0.4
psycopg[binary]>=3.1,<4

# RAG generation: LLM client + graph orchestration
langchain-openai==0.2.8
langgraph==0.2.76

# LangChain core (0.3 line, compatible with the packages above)
langchain-core>=0.3.70,<1.0
