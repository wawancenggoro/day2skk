# In-cluster address for the database: pgvector.pgvector.svc.cluster.local:5432
apiVersion: v1
kind: Service
metadata:
  name: pgvector
  labels:
    app: pgvector
spec:
  type: ClusterIP
  selector:
    app: pgvector
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
